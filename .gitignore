node_modules/
.env
*.log
.DS_Store
test/.datos-prueba.json

# Xabor Finanzas — e.firma y archivos sensibles (NUNCA subir a git)
certs/
*.cer
*.key
*.p12
*.pfx
storage/cfdi/
storage/documentos/

# Herramientas locales y archivos sensibles SAT
export-certs.bat
export-certs.ps1
validate-sat.bat
validate-sat-result.txt
cert_b64.txt
key_b64.txt
test/.preview-ticket-*.html

# Cola local de Xabor Edge: es estado del equipo del restaurante, nunca del repo.
edge/datos/
edge/.env

# Salida de la sonda de diagnostico: es un resultado local, no codigo.
probe-system-result.txt

# Staging del servicio de Windows: se arma con un comando, no viaja al repo.
staging-service/
