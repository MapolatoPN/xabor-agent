diff --git a/src/services/pagosService.js b/src/services/pagosService.js
index a203ccc..55f8fb9 100644
--- a/src/services/pagosService.js
+++ b/src/services/pagosService.js
@@ -291,7 +291,35 @@ async function resolverIntentoDePago({ negocioId, pedidoId, descripcion, idempot
   if (registro.metadata_sanitizada?.anomalia === 'creacion_ambigua' && !registro.referencia_externa) {
     if (capacidades.recuperaCreacionPorReferencia && adaptador.buscarCheckoutPorReferencia) {
       const credenciales = await obtenerCredencialesPagoDescifradas(negocioId, principal.proveedor);
-      const encontrado = await adaptador.buscarCheckoutPorReferencia(referenciaInterna, credenciales);
+      // La documentacion de Mercado Pago no garantiza read-after-write en el
+      // search. Una busqueda vacia INMEDIATAMENTE despues de una respuesta
+      // perdida no demuestra que la preferencia no exista: puede no estar
+      // indexada todavia. Se reintenta con espera y, si sigue sin aparecer, NO
+      // se crea otra: en dinero, incertidumbre es fail closed.
+      const intentos = Number(process.env.XABOR_PAGOS_BUSQUEDA_INTENTOS) || 3;
+      const esperaMs = Number(process.env.XABOR_PAGOS_BUSQUEDA_ESPERA_MS) || 1500;
+      let encontrado = null;
+      for (let i = 0; i < intentos && !encontrado; i++) {
+        if (i > 0) await new Promise(r => setTimeout(r, esperaMs));
+        encontrado = await adaptador.buscarCheckoutPorReferencia(referenciaInterna, credenciales);
+      }
+      if (encontrado?.ambiguo) {
+        await marcarAnomaliaPago(registro.id, negocioId, 'preferencias_duplicadas',
+          `el proveedor tiene ${encontrado.ids.length} checkouts con la misma referencia`);
+        throw new CreacionAmbiguaError(pedidoId, principal.proveedor);
+      }
+      if (encontrado?.ajena) {
+        await marcarAnomaliaPago(registro.id, negocioId, 'preferencia_ajena',
+          `la preferencia ${encontrado.preferenciaId} lleva la referencia ${encontrado.referenciaRecibida}`);
+        throw new CreacionAmbiguaError(pedidoId, principal.proveedor);
+      }
+      if (!encontrado) {
+        // El proveedor no la reporta tras varios intentos. Podria no existir...
+        // o podria existir y no estar visible. No se manda otro POST.
+        await marcarAnomaliaPago(registro.id, negocioId, 'creacion_ambigua_sin_resolver',
+          `${intentos} busquedas por referencia no encontraron el checkout: puede existir sin estar indexado`);
+        throw new CreacionAmbiguaError(pedidoId, principal.proveedor);
+      }
       if (encontrado) {
         // Existía: se adopta, no se crea nada. El checkout del cliente es ese.
         await actualizarPagoCreado(registro.id, {
@@ -309,7 +337,6 @@ async function resolverIntentoDePago({ negocioId, pedidoId, descripcion, idempot
           recuperadoTrasAmbiguedad: true,
         };
       }
-      // La búsqueda respondió que no existe: crear ahora sí es seguro.
     } else {
       throw new CreacionAmbiguaError(pedidoId, principal.proveedor);
     }
diff --git a/src/services/providers/mercadoPagoProvider.js b/src/services/providers/mercadoPagoProvider.js
index de1e580..4ac97bb 100644
--- a/src/services/providers/mercadoPagoProvider.js
+++ b/src/services/providers/mercadoPagoProvider.js
@@ -9,8 +9,9 @@
  * preferencia y verifica el resultado.
  *
  * Superficie usada de la API de Mercado Pago:
- *   POST /checkout/preferences         → crea la preferencia, devuelve init_point
- *   GET  /checkout/preferences/search  → recupera una preferencia por referencia
+ *   POST /checkout/preferences          → crea la preferencia, devuelve init_point
+ *   GET  /checkout/preferences/search   → SOLO da ids por external_reference
+ *   GET  /checkout/preferences/{id}     → external_reference real + init_point
  *   GET  /v1/payments/:id              → estado real de un pago
  *
  * IDEMPOTENCIA EN LA CREACIÓN: la referencia oficial de "Create preference"
@@ -270,17 +271,51 @@ export function getCapabilities() {
  * cobrable -- se pregunta. La busqueda cubre los ultimos 90 dias, que es de
  * sobra para un checkout cuya respuesta se perdio hace segundos.
  */
+/**
+ * ¿Existe ya una preferencia para esta referencia?
+ *
+ * Es la salida del caso ambiguo: Xabor mando el POST y no supo si llego a
+ * crearse. En vez de reintentar a ciegas -- que crearia un segundo checkout
+ * cobrable -- se pregunta.
+ *
+ * SON DOS PASOS, y la razon esta en la documentacion oficial: la respuesta de
+ * /checkout/preferences/search devuelve por elemento id, client_id,
+ * collector_id, date_created, expiration_date_from/to, items, marketplace,
+ * site_id y sponsor_id. NO devuelve init_point NI external_reference. Asi que
+ * el search solo sirve para obtener el id; la URL y la verificacion de que esa
+ * preferencia es de ESTA fila salen del GET individual.
+ *
+ * Devuelve:
+ *   null                              -> el search no encontro nada
+ *   { ambiguo: true, ids }            -> varias coincidencias: jamas elements[0]
+ *   { preferenciaId, url, referenciaInterna }
+ */
 export async function buscarCheckoutPorReferencia(referenciaInterna, credenciales) {
   const token = exigirToken(credenciales);
   const r = await pedir(
     `/checkout/preferences/search?external_reference=${encodeURIComponent(referenciaInterna)}`,
     { token });
-  const elem = (r?.elements || r?.results || [])[0];
-  if (!elem) return null;
+  const elementos = r?.elements || r?.results || [];
+  if (!elementos.length) return null;
+  if (elementos.length > 1) {
+    // Dos preferencias con la misma referencia es una anomalia: significa que
+    // hubo dos creaciones. Elegir una seria esconder la otra.
+    return { ambiguo: true, ids: elementos.map(e => e.id).filter(Boolean) };
+  }
+
+  const id = elementos[0]?.id;
+  if (!id) return null;
+  const detalle = await pedir(`/checkout/preferences/${encodeURIComponent(id)}`, { token });
+  // El GET individual SI trae external_reference: es lo unico que ata esa
+  // preferencia a nuestra fila. Sin coincidencia, no se adopta nada.
+  if (!detalle || detalle.external_reference !== referenciaInterna) {
+    return { ajena: true, preferenciaId: id, referenciaRecibida: detalle?.external_reference || null };
+  }
   return {
-    preferenciaId: elem.id || null,
-    url: elem.init_point || elem.sandbox_init_point || null,
-    referenciaExterna: elem.id || null,
+    preferenciaId: id,
+    url: detalle.init_point || detalle.sandbox_init_point || null,
+    referenciaExterna: id,
+    referenciaInterna: detalle.external_reference,
   };
 }
 
diff --git a/test/fase-pagos-obligacion-financiera.mjs b/test/fase-pagos-obligacion-financiera.mjs
index d0610da..fde5b72 100644
--- a/test/fase-pagos-obligacion-financiera.mjs
+++ b/test/fase-pagos-obligacion-financiera.mjs
@@ -98,17 +98,35 @@ let mpCortaRespuesta = false;
 const PREFERENCIAS = new Map();      // prefId -> { external_reference }
 const PAGOS_MP = new Map();
 let busquedasPreferencia = 0;
+let getsPreferencia = 0;
 const mpMock = createServer((req, res) => {
   if (req.url.startsWith('/checkout/preferences/search')) {
     busquedasPreferencia++;
     const ref = new URL(req.url, 'http://x').searchParams.get('external_reference');
+    // Forma DOCUMENTADA del search: los elementos NO traen init_point ni
+    // external_reference. Solo sirven para obtener el id.
     const elements = [...PREFERENCIAS.entries()]
-      .filter(([, v]) => v.external_reference === ref)
-      .map(([id]) => ({ id, init_point: `https://mp.test/checkout/${id}` }));
+      .filter(([, v]) => v.external_reference === ref && !v.oculta)
+      .map(([id]) => ({ id, client_id: '1', collector_id: 1, site_id: 'MLM',
+                        date_created: '2026-08-17T00:00:00.000Z', items: [] }));
     res.writeHead(200, { 'Content-Type': 'application/json' });
     res.end(JSON.stringify({ elements }));
     return;
   }
+  const detalle = /^\/checkout\/preferences\/([^/?]+)$/.exec(req.url);
+  if (detalle && req.method === 'GET') {
+    getsPreferencia++;
+    const pref = PREFERENCIAS.get(decodeURIComponent(detalle[1]));
+    if (!pref) { res.writeHead(404); res.end('{}'); return; }
+    // El GET individual SI trae external_reference e init_point.
+    res.writeHead(200, { 'Content-Type': 'application/json' });
+    res.end(JSON.stringify({
+      id: decodeURIComponent(detalle[1]),
+      external_reference: pref.referenciaDetalle || pref.external_reference,
+      init_point: `https://mp.test/checkout/${decodeURIComponent(detalle[1])}`,
+    }));
+    return;
+  }
   if (req.url.startsWith('/checkout/preferences')) {
     let cuerpo = '';
     req.on('data', c => { cuerpo += c; });
@@ -560,6 +578,81 @@ try {
     assert.strictEqual((await filas(folio)).length, 1, 'se crearon filas de más');
   });
 
+  await t('2f. el Search NO trae la URL: se recupera con el GET individual', async () => {
+    // La documentacion del search no incluye init_point ni external_reference.
+    // Si la adopcion funcionara sin el GET individual, seria porque el mock
+    // inventa campos que la API real no da.
+    assert.ok(getsPreferencia > 0, 'nunca se consultó la preferencia individual');
+    const f = (await filas('OB-0011'))[0];
+    assert.ok(String(f.url || '').includes('mp.test/checkout/'),
+      'la URL adoptada no salió del GET individual');
+  });
+
+  await t('2g. search vacío tras la respuesta perdida: NO se crea otra preferencia', async () => {
+    // Sin garantía de read-after-write, "no aparece" no demuestra "no existe".
+    const folio = 'OB-0017';
+    await pedido(folio, 388);
+    TOKEN_MP = await conectarMP();
+    const antes = checkoutsMP;
+    mpCortaRespuesta = true;
+    await assert.rejects(() => crearEnlace(folio));
+    mpCortaRespuesta = false;
+    // La preferencia existe pero el proveedor todavía no la indexa.
+    for (const [, v] of PREFERENCIAS) if (v.external_reference && !v.oculta) v.oculta = true;
+
+    process.env.XABOR_PAGOS_BUSQUEDA_INTENTOS = '2';
+    process.env.XABOR_PAGOS_BUSQUEDA_ESPERA_MS = '50';
+    await assert.rejects(() => crearEnlace(folio), e => e.code === 'CREACION_AMBIGUA',
+      'creó otra preferencia con la búsqueda vacía');
+    delete process.env.XABOR_PAGOS_BUSQUEDA_INTENTOS;
+    delete process.env.XABOR_PAGOS_BUSQUEDA_ESPERA_MS;
+
+    assert.strictEqual(checkoutsMP - antes, 1, 'se creó una preferencia de más');
+    const f = (await filas(folio))[0];
+    assert.strictEqual(f.metadata_sanitizada.anomalia, 'creacion_ambigua_sin_resolver');
+    for (const [, v] of PREFERENCIAS) delete v.oculta;
+  });
+
+  await t('2h. dos preferencias con la misma referencia: anomalía, jamás elements[0]', async () => {
+    const folio = 'OB-0018';
+    await pedido(folio, 415);
+    TOKEN_MP = await conectarMP();
+    const antes = checkoutsMP;
+    mpCortaRespuesta = true;
+    await assert.rejects(() => crearEnlace(folio));
+    mpCortaRespuesta = false;
+    // Se fabrica una SEGUNDA preferencia con la misma referencia.
+    const f0 = (await filas(folio))[0];
+    PREFERENCIAS.set('pref-ob-duplicada', { external_reference: f0.referencia_interna });
+
+    await assert.rejects(() => crearEnlace(folio), e => e.code === 'CREACION_AMBIGUA',
+      'eligió una de dos preferencias duplicadas');
+    assert.strictEqual(checkoutsMP - antes, 1, 'creó otra preferencia sobre una ambigüedad');
+    assert.strictEqual((await filas(folio))[0].metadata_sanitizada.anomalia, 'preferencias_duplicadas');
+    PREFERENCIAS.delete('pref-ob-duplicada');
+  });
+
+  await t('2i. el GET individual devuelve OTRA referencia: no se adopta', async () => {
+    const folio = 'OB-0019';
+    await pedido(folio, 452);
+    TOKEN_MP = await conectarMP();
+    mpCortaRespuesta = true;
+    await assert.rejects(() => crearEnlace(folio));
+    mpCortaRespuesta = false;
+    const f = (await filas(folio))[0];
+    // El proveedor devuelve, para ese id, una referencia distinta.
+    for (const [, v] of PREFERENCIAS) {
+      if (v.external_reference === f.referencia_interna) v.external_reference_real_distinta = true;
+    }
+    const id = [...PREFERENCIAS.entries()].find(([, v]) => v.external_reference === f.referencia_interna)?.[0];
+    PREFERENCIAS.get(id).external_reference = f.referencia_interna;   // el search sí la encuentra
+    PREFERENCIAS.get(id).referenciaDetalle = 'otra-referencia-distinta';
+
+    await assert.rejects(() => crearEnlace(folio), e => e.code === 'CREACION_AMBIGUA',
+      'adoptó una preferencia cuya referencia no coincide');
+    assert.strictEqual((await filas(folio))[0].metadata_sanitizada.anomalia, 'preferencia_ajena');
+  });
+
   await t('2e. las capacidades declaradas coinciden con lo que la documentación dice', async () => {
     // Si algún día un proveedor gana idempotencia real, esto cambia con la
     // implementación -- no antes. Declararla sin tenerla haría creer que la
